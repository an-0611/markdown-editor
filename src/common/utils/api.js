// import axios from 'axios';

// const articleRequest = axios.create({
//     baseURL: process.env.NODE_ENV === 'development' ? '/fakeData/' : 'https://an-0611/markdown-editor/',
// });

// export const getAllArticles = () => articleRequest.get('/All');

// export const getArticle = id => articleRequest.get(`/article/${id}`);

// export const updateArticle = (id, article) => articleRequest.put(`/article/${id}`, article);

// export const createArticle = article => articleRequest.post('/create/', article);